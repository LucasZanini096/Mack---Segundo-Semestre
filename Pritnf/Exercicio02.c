#include <stdio.h>

int main() {
  int evento = 5;
  char corrida = 'C';
  float tempo = 27.25;

  printf("O tempo vitorioso na eliminatória %c ", corrida);
  printf("da competição %d", evento);
  printf(" foi %f", tempo);

  return 0;
}